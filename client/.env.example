# Environment Variables for LOKAWAZ
VITE_API_BASE_URL=http://localhost:8000/api

# Note: Groq API key is now configured in the backend server (server/.env) for security
# The chatbot service now uses secure backend endpoints instead of exposing API keys in frontend
